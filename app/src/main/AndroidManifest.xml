<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bit.revalorando">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Revalorando">
        <activity android:name=".DescripcionArticuloActivity"></activity>
        <activity android:name=".ContactoActivity"></activity>
        <activity android:name=".AgregarArticuloActivity" />
        <activity android:name=".ListarOfertasRecibidasActivity" />
        <activity android:name=".OfertaActivity" />
        <activity android:name=".ListarArticuloActivity" />
        <activity android:name=".ListarMisTruequesActivity" />
        <activity android:name=".ListarSeleccionActivity" />
        <activity android:name=".AgregarTruequeActivity" />
        <activity android:name=".ListarTruequeActivity" />
        <activity android:name=".ListarUsuarioActivity" />
        <activity android:name=".AgregarUsuarioActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>